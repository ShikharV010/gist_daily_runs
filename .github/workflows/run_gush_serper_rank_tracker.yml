name: Run Gush Serper Rank Tracker Monday 9:00 AM IST

on:
  schedule:
    # 09:00 IST (UTC+5:30) == 03:30 UTC every Monday
    - cron: '30 3 * * 1'
  workflow_dispatch:

concurrency:
  group: gush-serper-rank-tracker
  cancel-in-progress: false

jobs:
  run-notebook:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # Swap with: pip install -r requirements.txt if you create one
          pip install jupyter nbconvert ipykernel pandas sqlalchemy psycopg2-binary requests python-dotenv

      - name: Execute notebook
        run: |
          jupyter nbconvert \
            --to notebook \
            --execute Gush_SEO_Serper_Rank_Tracker.ipynb \
            --output Gush_SEO_Serper_Rank_Tracker.output.ipynb \
            --ExecutePreprocessor.timeout=3600

      - name: Export HTML (optional)
        run: |
          jupyter nbconvert \
            --to html \
            --output Gush_SEO_Serper_Rank_Tracker.output.html \
            Gush_SEO_Serper_Rank_Tracker.output.ipynb

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: gush-serper-rank-tracker-output
          path: |
            Gush_SEO_Serper_Rank_Tracker.output.ipynb
            Gush_SEO_Serper_Rank_Tracker.output.html
